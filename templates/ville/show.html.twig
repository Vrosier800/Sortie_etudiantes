{% extends 'base.html.twig' %}

{% block title %}Ville{% endblock %}

{% block body %}
    <h1>Ville</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ ville.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ ville.nom }}</td>
            </tr>
            <tr>
                <th>CodePostal</th>
                <td>{{ ville.codePostal }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('ville_list') }}" class="text-primary text-decoration-none" >Retour à la liste des villes / </a>

    <a href="{{ path('ville_modifier', {'id': ville.id}) }}" class="text-primary text-decoration-none" >Modifier</a>

    {{ include('ville/_delete_form.html.twig') }}
{% endblock %}
